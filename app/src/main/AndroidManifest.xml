<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="muuttaa.myohemmin.realistisenelamansimulaattori">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="LockedOrientationActivity">
        <activity android:name=".ParentActivity"></activity>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="muuttaa.myohemmin.realistisenelamansimulaattori"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <activity android:name=".CreateScene" />
        <activity android:name=".CreateScenario" />
        <activity android:name=".SettingsActivity" />
        <activity
            android:name=".InitializeActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CreateScene"
            android:screenOrientation="portrait" />
        <activity
            android:name=".CreateScenario"
            android:screenOrientation="portrait" />
        <activity
            android:name=".SettingsActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ChooseScenarioActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ScenarioActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".GameOverActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>